# Netlify 배포 설정 — 정적 사이트(빌드 불필요)
[build]
  publish = "."
  command = ""

# 서버리스 함수 위치 (문의함 데이터 조회용)
[functions]
  directory = "netlify/functions"

# 관리자 페이지는 검색엔진 색인에서 제외
[[headers]]
  for = "/admin/*"
  [headers.values]
    X-Robots-Tag = "noindex"
